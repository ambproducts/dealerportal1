# Images Directory

Place the AmeriDex logo file here as:

```
ameridex-logo.png
```

The dealer portal HTML references this file at `images/ameridex-logo.png`.

Supported formats: PNG (preferred with transparent background) or SVG.
